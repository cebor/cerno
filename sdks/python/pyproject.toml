[project]
name = "cerno"
version = "0.1.0"
description = "Python client for cerno — noul, choice and score against a locally hosted model"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = ["httpx>=0.27"]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.23"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cerno"]

# Type checking is configured in ../../pyrightconfig.json, at the workspace root, because that
# is where an editor or language server starts. A [tool.pyright] section here is only read when
# pyright runs from this directory, which is not how anyone actually checks this package.

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
